# Working with Remote Repositories

A remote repository is the shared copy of your project on a server.

## Example:

```bash
git remote add origin git@github.com:example/repo.git
git push -u origin feature/api
```

## Result

Your branch is now available for other team members to fetch and review.

## Why it matters

Remote repositories are the source of truth for collaboration and deployment.
