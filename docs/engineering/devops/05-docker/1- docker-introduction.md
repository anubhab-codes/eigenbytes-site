# Docker Introduction

Docker packages applications into containers that run the same way everywhere.

## Story

Without containers, ?it works on my machine? means different OS, libraries, and environment variables.

## Example

```bash
docker run --rm hello-world
```

Result:

```text
Hello from Docker!
```

## Why it matters

A container bundles code, runtime, dependencies, and environment into one portable unit.
